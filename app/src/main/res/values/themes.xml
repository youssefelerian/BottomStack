<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ScalableView" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@android:color/holo_blue_dark</item>
    </style>

    <style name="Theme.ScalableView" parent="Base.Theme.ScalableView" />
</resources>